rules_version = '2';

// Permissive rules for local Firebase Emulator e2e.
// Tighten before deploying to production (owner-scoped auth.uid checks).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
